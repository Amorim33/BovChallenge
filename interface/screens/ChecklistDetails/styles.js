import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "row",
    backgroundColor: "#fff",
    padding: 10,
  },
  textContainer: {
    width: "80%",
  },
  label: {
    margin: 5,
    fontWeight: "bold",
    fontSize: 20,
    textAlign: "left",
  },
});

export default styles;
